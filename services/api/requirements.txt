fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.18
python-dotenv>=1.0.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
boto3>=1.35.0
python-magic>=0.4.27

# --- Audio analysis (local OSS models — no second API key) ---
# Essentia ships the MIR algorithms (BPM, key/scale, loudness) and, when the
# optional pretrained models are present, TensorFlow-backed genre/mood.
essentia-tensorflow>=2.1b6.dev1110
# CLAP joint audio<->text embeddings for similarity + semantic search.
laion-clap>=1.1.6
# CLAP's runtime backend: laion-clap imports torch/torchvision at analyze/embed
# time but doesn't always resolve them transitively, so declare them explicitly.
torch>=2.12.0
torchvision>=0.27.0
# Vector index: ships as a single NumPy `.npz` manifest built locally and synced
# to B2 via the repo S3 client (reproducible from a fresh clone; every byte stays
# on the custom-UA client). `lancedb` is reserved for a future ANN transport and
# is intentionally NOT a runtime dependency yet — see docs/features/embedding-index.md
# and the tech-debt tracker. Add it back when the LanceDB path is implemented.
# Audio decode/resample for feeding CLAP, and numeric ops for the index.
librosa>=0.10.2
soundfile>=0.12.1
numpy>=1.26.0

# Dev / quality
ruff>=0.8.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
